This folder contains nodes which can be reused between objects without code
modification. They are intended to be placed by instantiating the scenes found
next to the scripts, NOT via the scene tree's "right click > add child node"
interface.
